import { useState, useCallback } from 'react'
import { Plus, Trash2, ChevronDown } from 'lucide-react'
import { uid } from './data'
import type { CommercialSettings as CS, AdditionalCharge } from './types'
function Collapsible({ title, children, open: d = false }: { title: string; children: React.ReactNode; open?: boolean }) { const [open, setOpen] = useState(d); return <div className="nr-collapsible"><button type="button" onClick={() => setOpen(!open)} style={{ width: '100%', textAlign: 'left', padding: '18px 20px', cursor: 'pointer', display: 'flex', alignItems: 'center', justifyContent: 'space-between', background: 'transparent', border: 'none', fontWeight: 600, fontSize: '15px', color: '#2c232e', fontFamily: 'inherit', userSelect: 'none' }}><span>{title}</span><ChevronDown size={14} style={{ color: '#6b7280', transform: open ? 'rotate(180deg)' : 'none', transition: 'transform 0.2s' }} /></button>{open && <div style={{ padding: '0 20px 18px' }}>{children}</div>}</div> }
function Segment({ options, value, onChange }: { options: { value: string; label: string }[]; value: string; onChange: (v: string) => void }) { return <div className="nr-segment">{options.map(o => <button key={o.value} type="button" onClick={() => onChange(o.value)} className={`nr-seg-item ${value === o.value ? 'nr-seg-active' : ''}`}>{o.label}</button>)}</div> }
export function CommercialSettings({ settings, onUpdate }: { settings: CS; onUpdate: (s: CS) => void }) {
  const addCharge = useCallback(() => onUpdate({ ...settings, charges: [...settings.charges, { id: uid(), label: 'Charge', value: 0, taxable: false }] }), [settings])
  const updateCharge = useCallback((id: string, patch: Partial<AdditionalCharge>) => onUpdate({ ...settings, charges: settings.charges.map(c => c.id === id ? { ...c, ...patch } : c) }), [settings])
  const removeCharge = useCallback((id: string) => onUpdate({ ...settings, charges: settings.charges.filter(c => c.id !== id) }), [settings])
  return (
    <Collapsible title="Commercial Settings">
      <div className="nr-field"><label className="nr-label">Payment Terms</label><select value={settings.paymentTerms} onChange={e => onUpdate({ ...settings, paymentTerms: e.target.value })} className="nr-select"><option value="">Select terms</option>{['Due on Receipt', 'Net 7', 'Net 14', 'Net 30', 'Net 60'].map(t => <option key={t} value={t}>{t}</option>)}</select></div>
      <div className="nr-field"><div className="nr-toggle-wrap"><span className="nr-label" style={{ margin: 0 }}>Discount</span><button type="button" onClick={() => onUpdate({ ...settings, discount: { ...settings.discount, enabled: !settings.discount.enabled } })} className={`nr-switch ${settings.discount.enabled ? 'nr-switch-on' : ''}`} /></div>{settings.discount.enabled && <div style={{ marginTop: '10px' }}><div className="nr-field-row"><div className="nr-field"><label className="nr-label">Value</label><input type="number" value={settings.discount.value || ''} onChange={e => onUpdate({ ...settings, discount: { ...settings.discount, value: parseFloat(e.target.value) || 0 } })} min="0" step="0.01" className="nr-input nr-input-compact" /></div><div className="nr-field"><label className="nr-label">Type</label><Segment options={[{ value: 'percentage', label: '%' }, { value: 'flat', label: 'Flat' }]} value={settings.discount.type} onChange={v => onUpdate({ ...settings, discount: { ...settings.discount, type: v as 'percentage' | 'flat' } })} /></div></div><div className="nr-field" style={{ marginTop: '10px' }}><label className="nr-label">Timing</label><Segment options={[{ value: 'beforeTax', label: 'Before Tax' }, { value: 'afterTax', label: 'After Tax' }]} value={settings.discount.timing} onChange={v => onUpdate({ ...settings, discount: { ...settings.discount, timing: v as 'beforeTax' | 'afterTax' } })} /></div></div>}</div>
      <div className="nr-field"><div className="nr-toggle-wrap"><span className="nr-label" style={{ margin: 0 }}>VAT</span><button type="button" onClick={() => onUpdate({ ...settings, vat: { ...settings.vat, enabled: !settings.vat.enabled } })} className={`nr-switch ${settings.vat.enabled ? 'nr-switch-on' : ''}`} /></div>{settings.vat.enabled && <div style={{ marginTop: '10px' }}><label className="nr-label">Rate (%)</label><input type="number" value={settings.vat.rate || ''} onChange={e => onUpdate({ ...settings, vat: { ...settings.vat, rate: parseFloat(e.target.value) || 0 } })} min="0" step="0.01" className="nr-input nr-input-compact" /></div>}</div>
      <div className="nr-field"><div className="nr-toggle-wrap"><span className="nr-label" style={{ margin: 0 }}>WHT</span><button type="button" onClick={() => onUpdate({ ...settings, wht: { ...settings.wht, enabled: !settings.wht.enabled } })} className={`nr-switch ${settings.wht.enabled ? 'nr-switch-on' : ''}`} /></div>{settings.wht.enabled && <div style={{ marginTop: '10px' }}><div className="nr-field-row"><div className="nr-field"><label className="nr-label">Rate</label><input type="number" value={settings.wht.rate || ''} onChange={e => onUpdate({ ...settings, wht: { ...settings.wht, rate: parseFloat(e.target.value) || 0 } })} min="0" step="0.01" className="nr-input nr-input-compact" /></div><div className="nr-field"><label className="nr-label">Unit</label><Segment options={[{ value: 'percentage', label: '%' }, { value: 'flat', label: 'Flat' }]} value={settings.wht.unit} onChange={v => onUpdate({ ...settings, wht: { ...settings.wht, unit: v as 'percentage' | 'flat' } })} /></div></div></div>}</div>
      <div className="nr-field"><div style={{ display: 'flex', justifyContent: 'space-between', alignItems: 'center', marginBottom: '8px' }}><span className="nr-label" style={{ margin: 0 }}>Additional Charges</span><button type="button" onClick={addCharge} className="nr-btn nr-btn-sm">+ Add</button></div>{settings.charges.map(c => <div key={c.id} className="nr-charge-row"><input type="text" value={c.label} onChange={e => updateCharge(c.id, { label: e.target.value })} placeholder="Label" className="nr-input nr-input-compact" style={{ flex: 2, minWidth: '120px' }} /><input type="number" value={c.value || ''} onChange={e => updateCharge(c.id, { value: parseFloat(e.target.value) || 0 })} min="0" step="0.01" className="nr-input nr-input-compact" style={{ flex: 1, minWidth: '80px' }} /><span onClick={() => updateCharge(c.id, { taxable: !c.taxable })} className={`nr-charge-status ${c.taxable ? 'nr-charge-tax' : 'nr-charge-nontax'}`}>{c.taxable ? 'Taxable' : 'Non-taxable'}</span><button type="button" onClick={() => removeCharge(c.id)} style={{ width: '36px', height: '36px', padding: 0, border: 'none', background: 'none', borderRadius: '9999px', cursor: 'pointer', display: 'flex', alignItems: 'center', justifyContent: 'center', color: '#6b7280', fontSize: '18px', flexShrink: 0 }}>&#10005;</button></div>)}</div>
    </Collapsible>
  )
}
